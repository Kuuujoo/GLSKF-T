# 协作规则

- 代码保持简洁，优先写直接、清楚、可运行的实现。
- 变量名和函数名要短，避免无意义的长名字。
- 注释和 `fprintf` 输出使用中文。
- 不要堆重复的 `if exist`、`if isfile`、`if isfolder`，能封装就封装。
- 实验脚本按“参数、数据、输出、搜索、保存”的顺序组织。
- 主要实验脚本不要在末尾堆本地子函数；优先模仿现有图像实验脚本，从上到下直接写清楚。
- 不要为了炫技拆出一堆复杂函数，能少写就少写。
- 修改算法时必须保持实验含义清楚，不能把对照实验偷偷改成另一个模型。
